// Import modules
import chat from './chat';

export const state = () => ({
  // Root state if needed
});

export const mutations = {
  // Root mutations if needed
};

export const actions = {
  // Root actions if needed
};

export const getters = {
  // Root getters if needed
};

// Register modules
export const modules = {
  chat
};